node_modules/
dist/
.env
.DS_Store

# Article collection snapshots are generated local output by default.
# Commit only the snapshots that directly support a reviewed/published report.
data/articles/**

AGENTS.md

# User-customized source config (see sources.example.yaml)
sources.yaml
